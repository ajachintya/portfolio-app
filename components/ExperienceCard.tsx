import React, { ReactElement } from 'react'

interface Props {
    
}

export default function ExperienceCard({}: Props): ReactElement {
    return (
        <div>
            Card
        </div>
    )
}
